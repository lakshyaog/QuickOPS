# ── Dev / CI dependencies (not installed in the production Docker image) ───────
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0          # required by FastAPI TestClient (async transport)
flake8==7.0.0
